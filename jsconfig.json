{ 
  "compilerOptions": {
    // 解析非相关模块名称的基础目录
    "baseUrl": "./",
    // 解决项目中使用@作为路径别名，导致vscode无法跳转文件的问题
    "paths": {
        "@/*": ["src/*"]
    }
  },
  // 提高 IDE 性能
  "exclude": ["node_modules", "dist"]
}